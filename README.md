Run a command every specified duration.

```
$ run-every -h
Usage: run-every <duration> <program path> [program args...]
	duration is a string accepted by Go's time.ParseDuration (https://golang.org/pkg/time/#ParseDuration)
```
